<div class="container">
  <div class="row">
    <div class="col-md-6 col-12 mx-auto " style="border-right: grey 2px solid;">
      <h1 class="h4"><%= t('.Maibee_Market') %></h1>
      <%= render 'shared/limit_order_link'%>
      <div class="overflow-auto">
        <table>
          <thead>
            <tr>
              <td><%= t('.currency') %></td>
              <td><%= t('.amount') %></td>
              <td><%= t('.sell_price') %></td>
              <td><%= t('.total_price') %></td>
              <td><%= t('.order_number')%></td>
              <td></td>
            </tr>
          </thead>  
          <tbody> 
            <tbody id="new_order"></tbody>
            <% @limit_orders.each do |order|%>
              <tr id="market<%= order.id%>">
                <%= render 'shared/coin_icon', order: order%>
                <td><%= order.amount%></td>
                <td><%= order.sell_price.to_currency%></td>
                <td><%= order.price.to_currency%></td>
                <td><%= order.num[0..2]%></td>
                <% if order.user_id == current_user.id%>
                <td><%=t('.my_order')%></td>
                <% else %>
                <td><%= link_to t('.buy'), bit_market_path(order.id), method: 'post'%></td>
                <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  
    <div class="col-md-6 col-12 mx-auto vh-100 overflow-auto">
    </div>
  </div>
</div>
