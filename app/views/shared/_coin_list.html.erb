<ul class="list-group">
  <% currencies.each do |currency| %>
  <li class="list-group-item d-flex justify-content-between align-items-center">
    <span>
      <%= currency.name %>
    </span>
    <span>
      <%= currency.last_rate %>
    </span>
    <span class="">
      <%= link_to 'Buy', exchange_path(currency), class: 'btn btn-primary btn-sm' %>
    </span>
  </li>
<% end %>
</ul>
