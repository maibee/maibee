<canvas id="chart-<%= currency.codename %>" class="price-chart"></canvas>
<script>
var currencies_data = [{
    data: <%= currency.latest_prices.last(20).map{|x| x.price} %>,
    label: 'SPQ',
    fill: false
  },] 
var chart_label = <%= currency.latest_prices.last(20).map{|x| x.created_at.hour }%>
</script>
